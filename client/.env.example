# Optional: only needed when the Vite dev server and API run on different hosts.
# If unset, the client uses the current page origin (and Vite proxies API routes using server/.env PORT).
# VITE_SERVER_URL=http://localhost:8080
